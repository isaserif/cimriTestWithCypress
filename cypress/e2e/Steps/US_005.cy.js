describe('Kullanici Girişi ve Çikişi', () => {
    it('Giris dogrulanir', () => {
        // Kullanıcı giriş sayfasına erişilir.
        // Geçerli bir kullanıcı adı ve şifre girilir.
        // Kullanıcının başarılı bir şekilde giriş yaptığı doğrulanır.
        // Kullanıcı hesabına erişim sağlanır ve hesap bilgileri doğrulanır.
        // Kullanıcı oturumu kapatılır ve çıkış yapıldığı doğrulanır.
    });
    it('Kullanici hesap bilgileri ve dogrulamasi', () => {
        // Kullanıcı giriş sayfasına erişilir.
        // Geçerli bir kullanıcı adı ve şifre girilir.
        // Kullanıcı hesabına erişim sağlanır ve hesap bilgileri doğrulanır.
    });
    
    it('Kullanici oturum kapatir ve cikis yaptigi dogrulanir', () => {
         // Kullanıcı giriş sayfasına erişilir.
        // Geçerli bir kullanıcı adı ve şifre girilir.
        // Kullanıcı oturumu kapatılır ve çıkış yapıldığı doğrulanır.
    });
});
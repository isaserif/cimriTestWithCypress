describe('Filtreleme ve siralama', () => {
    it('filtreleme', () => {
        // Kategori sayfasında veya arama sonuçlarında bir filtre veya sıralama seçeneği seçilir.
        // Ürünlerin filtrelenmiş olduğu doğrulanır.
    });
    it('siralama', () => {
         // Kategori sayfasında veya arama sonuçlarında bir filtre veya sıralama seçeneği seçilir.
        // Ürünlerin sıralanmış olduğu doğrulanır.
    });
});